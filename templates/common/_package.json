{
  "name": "<%= slugname %>",
  "description": "<%= description %>",
  "version": "<%= version %>",
  "keywords": [
    "sassdoc-theme"
  ],
  "dependencies": {<% if (useFilter) { %>
    "sassdoc-filter": "^1.0.0",<% } %><% if (useIndexer) { %>
    "sassdoc-indexer": "^1.0.0",<% } %>
    "extend": "^1.0.0",
    "themeleon": "^1.0.0"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
